query Beats($limit: Int!, $cursor: String) {
    beats(limit: $limit, cursor: $cursor) {
        hasMore
        beats {
            id
            title
            genre
            bpm
            key
            tags
            creatorId
            createdAt
            updatedAt
        }
    }
}
