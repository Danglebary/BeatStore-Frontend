mutation CreateBeat($options: CreateBeatInput!) {
    createBeat(options: $options) {
        errors {
            field
            message
        }
        beat {
            id
            title
            bpm
            key
            genre
            createdAt
            updatedAt
        }
    }
}
